{
  "name": "harfist",
  "version": "0.1.0",
  "private": true,
  "description": "Harfist Turkish word games packaged with Capacitor for Android.",
  "scripts": {
    "build:web": "node scripts/build-www.js",
    "cap:sync": "npm run build:web && npx cap sync android",
    "android:add": "npm run build:web && npx cap add android",
    "android:open": "npm run build:web && npx cap sync android && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
